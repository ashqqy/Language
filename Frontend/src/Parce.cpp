#include "Parce.h"
#include "Tree.h"
#include "CustomAssert.h"

void GetProgram (tree_node_t** token_array, size_t* shift)
{
    CustomAssert (token_array != NULL);
    CustomAssert (shift       != NULL);
}